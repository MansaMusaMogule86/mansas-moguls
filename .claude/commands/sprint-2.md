create all missing public routes from PRD, add schema-aligned TS types, shared UI components, mock-data-driven Moguls/Projects pages, no Supabase/auth/dashboard, public visibility only, then run lint/build.

